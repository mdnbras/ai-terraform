# Testes

- `terraform fmt -check` e `validate` são baseline.
- Use validations e conditions para invariantes permanentes.
- Testes com plan/mocks são preferidos quando suficientes.
- Testes que aplicam recursos exigem autorização, budget e cleanup.

